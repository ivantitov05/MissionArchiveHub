spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/mission_archive
    username: postgres
    password: ghjuhju55
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

server:
  port: 8080

logging:
  level:
    org.springframework.web: DEBUG
    org.hibernate.SQL: DEBUG